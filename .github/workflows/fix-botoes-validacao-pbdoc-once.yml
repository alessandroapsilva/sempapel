name: Ajusta botoes e validacao PBdoc

on:
  push:
    branches:
      - release/11.5
    paths:
      - .github/workflows/fix-botoes-validacao-pbdoc-once.yml

permissions:
  contents: write

jobs:
  corrigir:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: release/11.5
          fetch-depth: 0
      - name: Aplicar
        run: python3 .github/scripts/fix-botoes-validacao-pbdoc.py
      - name: Commitar
        run: |
          git config user.name "ENFAS Bot"
          git config user.email "enfas@users.noreply.github.com"
          rm -f .github/workflows/fix-botoes-validacao-pbdoc-once.yml .github/scripts/fix-botoes-validacao-pbdoc.py
          git add -A
          git commit -m "Restaura Ver Documento e deixa obrigatorios so no modal"
          git push origin HEAD:release/11.5

# segunda execução após correção do script
