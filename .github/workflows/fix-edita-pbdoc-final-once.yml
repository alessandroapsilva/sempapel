name: Ajuste final edita PBdoc

on:
  push:
    branches:
      - release/11.5
    paths:
      - .github/workflows/fix-edita-pbdoc-final-once.yml

permissions:
  contents: write

jobs:
  corrigir:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: release/11.5
          fetch-depth: 0
      - name: Aplicar
        run: python3 .github/scripts/fix-edita-pbdoc-final.py
      - name: Commitar
        run: |
          git config user.name "ENFAS Bot"
          git config user.email "enfas@users.noreply.github.com"
          rm -f .github/workflows/fix-edita-pbdoc-final-once.yml .github/scripts/fix-edita-pbdoc-final.py
          git add -A
          git commit -m "Remove vermelho dos obrigatorios e restaura Ver Documento"
          git push origin HEAD:release/11.5
