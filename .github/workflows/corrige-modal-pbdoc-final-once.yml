name: Corrige modal PBdoc final

on:
  push:
    branches:
      - release/11.5
    paths:
      - .github/workflows/corrige-modal-pbdoc-final-once.yml

permissions:
  contents: write

jobs:
  corrigir:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: release/11.5
          fetch-depth: 0
      - name: Aplicar
        run: python3 .github/scripts/fix-modal-pbdoc-final.py
      - name: Commitar
        run: |
          git config user.name "ENFAS Bot"
          git config user.email "enfas@users.noreply.github.com"
          rm -f .github/workflows/corrige-modal-pbdoc-final-once.yml .github/scripts/fix-modal-pbdoc-final.py
          git add -A
          git commit -m "Faz modal de obrigatórios igual ao PBdoc"
          git push origin HEAD:release/11.5
